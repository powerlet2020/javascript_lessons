const routine = ['eat', 'learn','sleep']
        console.log(routine)

        const arr = [ 'eat','sleep',4 , true]
        console.log(arr)

        console.log(arr[0])
        console.log(arr.length)
        console.log(arr[arr.length-3])


        //   array methods ........
         arr.push('hamza')
        console.log(arr)

        routine[1]= 'power'
        console.log(routine)
        routine.pop()
        console.log(routine)

        const myarr = new Array('hamza', 'maths')
        console.log(myarr)